namespace ConstStrings
{
    public class Savables_ConstStrings
    {
        public const string fieldOfView_Settings = nameof(fieldOfView_Settings);
        public const string mouseSensitivity_Settings = nameof(mouseSensitivity_Settings);
        public const string masterVolume_Settings = nameof(masterVolume_Settings);
        public const string musicVolume_Settings = nameof(musicVolume_Settings);
        public const string effectsVolume_Settings = nameof(effectsVolume_Settings);
    }
}